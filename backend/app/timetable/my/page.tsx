"use client";

import TimetablePage from "../page";

/** Distraction-free route selected by teachers immediately after login. */
export default function MyTimetableFocusPage() {
  return <TimetablePage />;
}
